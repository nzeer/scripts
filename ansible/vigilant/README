# cron runs python script responsible every 15m? hourly? daily?

[bash?] Using ip scans as base
scaffolding
	- [python] format ip scans into inventory (yaml)
	- [python][ansible] run gather facts against the entire inventory
		- [python][ansible] write out gather facts results to disk
		- [python] store gather facts
			relational db?
				- redis?
				- mongo?
				- postgres?
				- sqlite?
	- build inventory files from db
		- segregate based on os
			- unknown
			- rhel 7x
			- rhel 8x
		- using world file for segmentation
		- commit in git
	- display facts on webpage
		- flask?
